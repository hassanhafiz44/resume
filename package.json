{
  "name": "resume",
  "dependencies": {
    "hono": "^4",
    "react": "^18",
    "react-dom": "^18",
    "react-router-dom": "^7.16.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  },
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build src/client/main.tsx --outdir public/dist --target browser --minify"
  }
}
